{
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs'); if(!fs.existsSync('src/scss')){console.log('skip sass (no src/scss)'); process.exit(0)}\" && sass src/scss:assets/css --style=compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.79.0"
  }
}